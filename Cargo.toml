[package]
name = "cohortc"
version = "0.1.0"
edition = "2021"
description = "Deterministic cohort compiler: named population definitions to dialect SQL"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"

[profile.release]
opt-level = 2
